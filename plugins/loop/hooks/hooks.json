{
  "description": "Loop plugin hooks - repeating prompt loop control via Stop hook",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "uv run --directory ${CLAUDE_PLUGIN_ROOT} python ${CLAUDE_PLUGIN_ROOT}/hooks/scripts/loop-stop-hook.py",
            "timeout": 7200,
            "statusMessage": "Checking loop..."
          }
        ]
      }
    ]
  }
}
