{
  "hooks": [
    {
      "name": "orchestrator-startup",
      "event": "on_session_start",
      "description": "Check orchestrator environment on Claude startup",
      "command": "python3 $HOME/dev/tools/tmux-orchestrator/lib/claude_control.py status 2>/dev/null || echo 'Orchestrator not configured'",
      "enabled": false
    },
    {
      "name": "agent-registration-check",
      "event": "on_command",
      "matcher": "/orc-*",
      "description": "Verify tmux is available before orchestrator commands",
      "command": "tmux has-session 2>/dev/null || echo 'Warning: No tmux sessions found'",
      "enabled": true
    }
  ]
}
